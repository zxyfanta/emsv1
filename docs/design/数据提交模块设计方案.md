# 数据提交模块设计方案

## 📊 现状分析

### 系统现有设计模式
根据最近10次commit和代码分析,系统采用了以下设计模式:

1. **功能开关模式** (`app.ems.features`)
   - `radiation-enabled`: 控制辐射设备功能显示
   - `environment-enabled`: 控制环境设备功能显示

2. **配置驱动模式** (`AlertProperties`)
   - 使用 `@ConfigurationProperties` 从yaml加载配置
   - 支持环境变量覆盖
   - 不同功能模块独立配置

3. **数据流向**
   ```
   MQTT设备 → RadiationMqttMessage → RadiationDeviceData(含GPS/BDS/LBS)
   ```

4. **权限设计**
   - ADMIN: 管理所有配置
   - USER: 查看企业自己的数据

---

## 🎯 数据提交模块需求

### 核心需求
- 向监管机构提交GPS相关数据
- 提交需要满足机构特定格式要求
- 需要配置管理提交参数(机构地址、认证信息等)
- 需要记录提交历史和状态

### 参考告警系统设计
告警系统的成功经验:
- ✅ 配置化 (AlertProperties)
- ✅ 多类型支持 (HIGH_CPM, OFFLINE, FAULT, LOW_BATTERY)
- ✅ 独立服务 (AlertService, AlertConfigService)
- ✅ 企业级隔离 (company_id)
- ✅ 前端配置页面

---

## 🏗️ 架构设计方案

### 方案A: 企业级配置 + 定时任务提交 (推荐) ⭐

#### 设计思想
每个企业独立配置提交参数,系统定时扫描并提交数据

#### 核心组件

```java
// 1. 实体设计
@Data
@Entity
@Table(name = "ems_data_submission_config")
public class DataSubmissionConfig {
    private Long id;
    private Long companyId;              // 关联企业

    // 提交类型
    @Enumerated(EnumType.STRING)
    private SubmissionType submissionType;  // GPS辐射数据, 环境数据

    // 是否启用
    private Boolean enabled;

    // 机构配置
    private String institutionName;       // 机构名称
    private String institutionUrl;        // 机构API地址
    private String institutionAuth;       // 认证信息(API Key/Token)
    private Map<String, String> headers;  // 自定义请求头

    // 提交配置
    @Enumerated(EnumType.STRING)
    private SubmissionFrequency frequency;  // HOURLY, DAILY, WEEKLY
    private String cronExpression;        // 自定义cron表达式
    private Integer batchSize;            // 每批提交数量

    // 数据过滤
    private String deviceFilter;          // 设备过滤条件
    private Boolean includeOffline;       // 是否包含离线设备数据

    // 数据转换规则
    private Map<String, String> fieldMapping;  // 字段映射关系
    private String dataFormatTemplate;    // 数据格式模板(JSON/XML)

    // 状态
    private LocalDateTime lastSubmissionAt;
    private LocalDateTime nextSubmissionAt;
    private Integer totalSubmissions;
    private Integer successCount;
    private Integer failureCount;

    // 审计字段
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
    private String createdBy;
    private String updatedBy;
}

// 2. 提交类型枚举
public enum SubmissionType {
    GPS_RADIATION_DATA,      // GPS辐射数据
    ENVIRONMENT_DATA,        // 环境数据
    DEVICE_STATUS,           // 设备状态
    ALERT_DATA              // 告警数据
}

// 3. 提交频率枚举
public enum SubmissionFrequency {
    HOURLY,    // 每小时
    DAILY,     // 每天
    WEEKLY,    // 每周
    CUSTOM     // 自定义cron
}

// 4. 提交历史记录
@Data
@Entity
@Table(name = "ems_data_submission_log")
public class DataSubmissionLog {
    private Long id;
    private Long companyId;
    private Long configId;

    @Enumerated(EnumType.STRING)
    private SubmissionType submissionType;

    private LocalDateTime submittedAt;
    private Integer recordCount;
    private String status;  // SUCCESS, FAILED, PARTIAL

    private String requestUrl;
    private String requestData;  // JSON格式的请求体
    private String responseData; // 响应数据

    private Integer httpStatus;
    private String errorMessage;
    private Long duration;  // 提交耗时(ms)

    private LocalDateTime createdAt;
}
```

#### 后端服务

```java
// 1. 配置服务
@Service
public class DataSubmissionConfigService {
    // CRUD操作
    public DataSubmissionConfig createConfig(DataSubmissionConfig config);
    public DataSubmissionConfig updateConfig(Long id, DataSubmissionConfig config);
    public void deleteConfig(Long id);
    public DataSubmissionConfig getConfig(Long id);

    // 企业级查询
    public List<DataSubmissionConfig> getCompanyConfigs(Long companyId);
    public DataSubmissionConfig getCompanyConfigByType(Long companyId, SubmissionType type);

    // 启用/禁用
    public void enableConfig(Long id);
    public void disableConfig(Long id);

    // 立即触发提交(测试用)
    public SubmissionResult triggerSubmission(Long configId);
}

// 2. 提交服务
@Service
public class DataSubmissionService {
    // 定时任务扫描并提交
    @Scheduled(cron = "0 */5 * * * ?")  // 每5分钟扫描一次
    public void scanAndSubmit();

    // 执行提交
    public SubmissionResult submitData(DataSubmissionConfig config);

    // 构建提交数据
    private Object buildSubmissionData(DataSubmissionConfig config);

    // GPS数据构建器
    private List<GpsSubmissionData> buildGpsData(
        DataSubmissionConfig config,
        LocalDateTime startTime,
        LocalDateTime endTime
    );

    // HTTP提交
    private HttpResponse sendToInstitution(
        DataSubmissionConfig config,
        Object data
    );

    // 记录提交日志
    private void logSubmission(SubmissionResult result);
}

// 3. 数据转换服务
@Service
public class DataTransformService {
    // 根据配置转换数据格式
    public Object transformData(
        List<RadiationDeviceData> rawData,
        DataSubmissionConfig config
    );

    // 应用字段映射
    private Map<String, Object> applyFieldMapping(
        RadiationDeviceData data,
        Map<String, String> mapping
    );

    // 应用模板
    private String applyTemplate(
        List<Map<String, Object>> data,
        String template
    );

    // GPS数据格式转换(度分 → 十进制)
    private Double convertGpsFormat(String gps);
}

// 4. 配置属性
@Component
@ConfigurationProperties(prefix = "app.ems.data-submission")
@Data
public class DataSubmissionProperties {
    private Boolean enabled = true;           // 全局开关
    private Integer maxRetryTimes = 3;        // 最大重试次数
    private Integer timeoutSeconds = 30;      // 超时时间
    private Boolean asyncMode = true;         // 异步模式
    private String defaultFormat = "JSON";    // 默认格式
}
```

#### Controller层

```java
@RestController
@RequestMapping("/data-submission")
public class DataSubmissionController {

    // ========== 配置管理 (ADMIN) ==========

    @GetMapping("/configs")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<ApiResponse<Page<DataSubmissionConfigResponse>>> listConfigs(
        @RequestParam Long companyId,
        @RequestParam(defaultValue = "0") int page
    );

    @PostMapping("/configs")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<ApiResponse<DataSubmissionConfigResponse>> createConfig(
        @Valid @RequestBody DataSubmissionConfigRequest request
    );

    @PutMapping("/configs/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<ApiResponse<DataSubmissionConfigResponse>> updateConfig(
        @PathVariable Long id,
        @Valid @RequestBody DataSubmissionConfigRequest request
    );

    @DeleteMapping("/configs/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<ApiResponse<Void>> deleteConfig(@PathVariable Long id);

    // ========== 企业配置 (USER) ==========

    @GetMapping("/current/configs")
    public ResponseEntity<ApiResponse<List<DataSubmissionConfigResponse>>> getMyConfigs();

    @GetMapping("/current/configs/{type}")
    public ResponseEntity<ApiResponse<DataSubmissionConfigResponse>> getMyConfigByType(
        @PathVariable SubmissionType type
    );

    // ========== 手动触发 ==========

    @PostMapping("/configs/{id}/trigger")
    public ResponseEntity<ApiResponse<SubmissionResultResponse>> triggerSubmission(
        @PathVariable Long id
    );

    // ========== 提交历史 ==========

    @GetMapping("/logs")
    public ResponseEntity<ApiResponse<Page<DataSubmissionLogResponse>>> listLogs(
        @RequestParam Long configId,
        @RequestParam(defaultValue = "0") int page,
        @RequestParam(defaultValue = "20") int size
    );

    @GetMapping("/logs/{id}")
    public ResponseEntity<ApiResponse<DataSubmissionLogResponse>> getLogDetail(
        @PathVariable Long id
    );

    // ========== 统计 ==========

    @GetMapping("/stats")
    public ResponseEntity<ApiResponse<SubmissionStatsResponse>> getStats(
        @RequestParam Long configId
    );
}
```

#### application.yaml配置

```yaml
app:
  ems:
    # 功能开关
    features:
      radiation-enabled: ${EMS_FEATURE_RADIATION:true}
      environment-enabled: ${EMS_FEATURE_ENVIRONMENT:true}
      data-submission-enabled: ${EMS_FEATURE_DATA_SUBMISSION:true}  # 新增

    # 数据提交配置
    data-submission:
      enabled: ${EMS_DATA_SUBMISSION_ENABLED:true}
      max-retry-times: 3
      timeout-seconds: 30
      async-mode: true
      default-format: JSON

      # GPS数据提交配置
      gps:
        default-institution: "环保监管部门"
        default-url: "https://api.example.com/submit"
        coordinate-format: DECIMAL  # DECIMAL(十进制) 或 DEGREE_MINUTE(度分)
        include-offline: false
        batch-size: 100
```

#### 前端页面

```vue
<!-- 1. 数据提交配置列表 (管理员) -->
<!-- src/views/data-submission/SubmissionConfigList.vue -->

<!-- 2. 数据提交配置表单 -->
<!-- src/views/data-submission/SubmissionConfigForm.vue -->

<!-- 3. 我的提交配置 (普通用户) -->
<!-- src/views/data-submission/MySubmissionConfig.vue -->

<!-- 4. 提交历史记录 -->
<!-- src/views/data-submission/SubmissionLog.vue -->

<!-- 5. 提交统计 -->
<!-- src/views/data-submission/SubmissionStats.vue -->
```

#### 路由配置

```javascript
// 管理员路由
export const adminRoutes = [
  // ...
  {
    path: '/data-submission/configs',
    name: 'DataSubmissionConfigList',
    component: () => import('@/views/data-submission/SubmissionConfigList.vue'),
    meta: {
      title: '数据提交配置',
      icon: 'Upload',
      category: '数据管理',
      order: 40
    }
  }
]

// 普通用户路由
export const userRoutes = [
  // ...
  {
    path: '/data-submission/my-configs',
    name: 'MySubmissionConfig',
    component: () => import('@/views/data-submission/MySubmissionConfig.vue'),
    meta: {
      title: '数据提交',
      icon: 'Upload',
      category: '数据管理',
      order: 32
    }
  }
]
```

---

## 🎨 数据格式转换示例

### 辐射GPS数据转换

```java
// 原始数据 (RadiationDeviceData)
{
  "deviceCode": "DEV001",
  "cpm": 123.5,
  "recordTime": "2025-01-15 14:30:45",
  "bdsLongitude": "1040634.56",  // 度分格式
  "bdsLatitude": "3012.78",
  "bdsUseful": 1
}

// 机构要求格式
{
  "deviceId": "DEV001",
  "radiationValue": 123.5,
  "longitude": 104.1096,     // 十进制
  "latitude": 30.2130,
  "positionValid": true,
  "timestamp": "2025-01-15T14:30:45+08:00",
  "dataSource": "BDS"
}

// 字段映射配置
{
  "deviceCode": "deviceId",
  "cpm": "radiationValue",
  "bdsLongitude": "longitude",
  "bdsLatitude": "latitude",
  "bdsUseful": "positionValid",
  "recordTime": "timestamp"
}
```

---

## 🔄 提交流程

```
定时任务(每5分钟)
    ↓
扫描启用的配置
    ↓
检查下次提交时间
    ↓
读取数据范围(上次提交时间 → 现在)
    ↓
查询设备数据(GPS/BDS/LBS)
    ↓
应用数据转换
    ↓
批量提交到机构API
    ↓
记录提交日志
    ↓
更新配置状态(下次提交时间)
```

---

## 📊 数据库表设计

```sql
-- 提交配置表
CREATE TABLE ems_data_submission_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    company_id BIGINT NOT NULL,
    submission_type VARCHAR(50) NOT NULL,
    enabled BOOLEAN DEFAULT TRUE,

    institution_name VARCHAR(200),
    institution_url VARCHAR(500),
    institution_auth VARCHAR(500),
    headers JSON,

    frequency VARCHAR(20),
    cron_expression VARCHAR(100),
    batch_size INT DEFAULT 100,

    device_filter VARCHAR(500),
    include_offline BOOLEAN DEFAULT FALSE,

    field_mapping JSON,
    data_format_template TEXT,

    last_submission_at DATETIME,
    next_submission_at DATETIME,
    total_submissions INT DEFAULT 0,
    success_count INT DEFAULT 0,
    failure_count INT DEFAULT 0,

    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by VARCHAR(50),
    updated_by VARCHAR(50),

    FOREIGN KEY (company_id) REFERENCES ems_company(id),
    INDEX idx_company_type (company_id, submission_type),
    INDEX idx_enabled (enabled)
);

-- 提交日志表
CREATE TABLE ems_data_submission_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    company_id BIGINT NOT NULL,
    config_id BIGINT NOT NULL,
    submission_type VARCHAR(50) NOT NULL,

    submitted_at DATETIME NOT NULL,
    record_count INT,
    status VARCHAR(20),

    request_url VARCHAR(500),
    request_data TEXT,
    response_data TEXT,

    http_status INT,
    error_message TEXT,
    duration BIGINT,

    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (config_id) REFERENCES ems_data_submission_config(id),
    INDEX idx_company_time (company_id, submitted_at),
    INDEX idx_status (status)
);
```

---

## 🎯 是否需要管理员管理?

### ✅ 需要 - 原因如下:

1. **配置复杂度高**
   - 机构API地址、认证信息需要管理员配置
   - 字段映射关系需要技术知识
   - 数据格式模板需要开发能力

2. **多企业隔离**
   - 不同企业可能提交到不同机构
   - 需要统一管理所有企业的配置

3. **安全考虑**
   - 认证信息(API Key/Token)不应暴露给普通用户
   - 需要防止恶意配置导致数据泄露

4. **故障排查**
   - 管理员需要查看所有提交历史
   - 需要全局统计和监控

### 👥 权限设计

| 功能 | ADMIN | USER |
|-----|-------|-----|
| 查看所有企业配置 | ✅ | ❌ |
| 创建/编辑/删除配置 | ✅ | ❌ |
| 启用/禁用配置 | ✅ | ❌ |
| 查看自己企业的配置 | ✅ | ✅ |
| 查看自己企业的提交历史 | ✅ | ✅ |
| 手动触发提交 | ✅ | ✅ |
| 查看提交统计 | ✅ | ✅(仅自己的) |

---

## 🚀 实施步骤

### Phase 1: 基础框架
1. 创建实体类和Repository
2. 实现基础的CRUD服务
3. 创建前端配置页面

### Phase 2: 数据提交
1. 实现定时任务扫描
2. 实现数据转换服务
3. 实现HTTP提交逻辑
4. 记录提交日志

### Phase 3: 优化增强
1. 添加重试机制
2. 添加失败告警
3. 添加统计报表
4. 性能优化

---

## 💡 与现有系统的契合度

| 特性 | 告警系统 | 数据提交系统 | 匹配度 |
|-----|---------|-------------|--------|
| 功能开关 | ✅ | ✅ | 100% |
| 配置属性 | ✅ | ✅ | 100% |
| 企业隔离 | ✅ | ✅ | 100% |
| 定时任务 | ✅ | ✅ | 100% |
| 日志记录 | ✅ | ✅ | 100% |
| 前端配置 | ✅ | ✅ | 100% |

**结论**: 完全契合现有架构设计! ✨

---

## 📝 总结

这个设计方案:
- ✅ **参考了告警系统的成功经验**
- ✅ **遵循了功能开关模式**
- ✅ **支持企业级隔离**
- ✅ **提供了灵活的配置管理**
- ✅ **具备完善的日志和监控**
- ✅ **权限设计清晰合理**

建议采用**方案A: 企业级配置 + 定时任务提交**,这是最适合你们系统的架构!
